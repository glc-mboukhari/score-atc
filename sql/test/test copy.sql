CREATE TABLE IF NOT EXISTS public.example_table (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    value INT,
    created_at DATE
);
CREATE TABLE IF NOT EXISTS public.example_table2 (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    value INT,
    created_at DATE
);
SELECT * FROM ann_online_nov_2023_april_2024 LIMIT 10;